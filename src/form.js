class form extends Component {
    state = { 
     }
    // Need a state for each of the form pages 
    render() { // render does all the html css shit, import css file 
        return ( );
    }
}
 
// big form render, smaller forms export 
export default form;